# C3 — LLM (Change Spec)

This filename is the jig template's expected name. The project authored the C3 spec under the broader name `c3-pipeline-spec.md` to reflect that the component owns the full processing pipeline (text-extract + classify + extract + retype + audit + eval), not just the LLM call sites.

**See `c3-pipeline-spec.md` in this directory.**
