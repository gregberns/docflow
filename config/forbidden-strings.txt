# Forbidden literal strings for the grepForbiddenStrings build check.
#
# Authority:
#   - .kerf/docflow/05-specs/c1-config-spec.md §3.6 (C1-R7) — stage names and
#     client slugs.
#   - .kerf/docflow/05-specs/c7-platform-spec.md §3.6 / C7-R13 — env-read
#     patterns and the .env filename.
# Consumer: C7-R5 grepForbiddenStrings Gradle task (see backend/build.gradle.kts).
#
# Two pattern categories. Both are exempt from scanning under
# com.docflow.config.**, in Flyway migration classes (out of source-set), and
# in test sources (sourceRoot scopes to src/main/java only).
#
# 1. Default section — matched as quoted Java string literals (the regex
#    wraps each line in "..."). Stage names, client slugs, the .env filename.
#
# 2. [bare-tokens] section — matched as raw substrings (no quote wrapping).
#    Env-access expressions whose appearance outside com.docflow.config.**
#    signals a config-boundary leak per C7-R13.
#
# Format:
#   - One literal per line, no surrounding quotes.
#   - A line consisting of '[<name>]' is a section header.
#   - Lines beginning with '#' are comments; blank lines are ignored.
#   - Leading/trailing whitespace on a literal line is trimmed before matching.

# Stage names (11)
Review
Manager Approval
Finance Approval
Attorney Approval
Billing Approval
Partner Approval
Project Manager Approval
Accounting Approval
Client Approval
Filed
Rejected

# Client slugs (3)
riverside-bistro
pinnacle-legal
ironworks-construction

# Client display-name variants (3)
Riverside Bistro
Pinnacle Legal Group
Ironworks Construction

# Env-file literal (1)
.env

[bare-tokens]
# Env-read expressions (C7-R13) — allowed only in com.docflow.config.**.
System.getenv
@Value
